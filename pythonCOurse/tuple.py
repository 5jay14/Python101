#  tuple = a collection which is ordered and unchangeable. USed to group together related data
student = ("Bro", 21, "male")

print(student.count("Bro"))  # shows how many times the element is in the tuple
print(student.index(21))  # gives the index number of the element
